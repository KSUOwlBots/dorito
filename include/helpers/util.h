
float clamp(float val, float min, float max);

int sign (double);

int sign (float);

double rad2deg(double);

double deg2rad(double);
const int DELAY_TIME = 10;



#ifndef UTILVARS_H_
#define UTILVARS_H_
extern int utilVar1 = 0;
#endif